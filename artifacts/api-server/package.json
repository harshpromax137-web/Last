{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "uv venv .venv --allow-existing && uv pip install --python .venv/bin/python -r requirements.txt",
    "dev": "pnpm run build && .venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8080}",
    "start": ".venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8080}",
    "typecheck": "python -m compileall -q Api Configuration Proto Utilities app.py info_service.py stats_service.py reference_renderer.py"
  }
}
